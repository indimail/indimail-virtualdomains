name: bogofilter Ubuntu, Mac OSX CI

on:
  push:
    pull_request:
      - main
      - master
    branches:
      - master
    paths:
      - '**/bogofilter-x/**'
      - .github/workflows/bogofilter-c-cpp.yml
      - '!**/bogofilter-x/debian/*'
      - '!**/bogofilter-x/doc/*'
      - '!**.md'
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: ${{ matrix.host }}-${{ matrix.config.name }}
    runs-on: ${{ matrix.host }}
    strategy:
      fail-fast: false
      matrix:
        host: [ubuntu-latest, macos-latest]
    
    steps:
    - name: extra_packages
      run: |
        if [ "${OS}" = "macos-latest" ] ; then brew install automake autoconf libtool pkgconfig berkeley-db; fi
        if [ "${OS}" = "ubuntu-latest" ] ; then sudo apt-get install gcc g++ automake autoconf libtool xmlto libgsl-dev bison flex libdb-dev; fi
      env:
        OS: ${{ matrix.host }}

    - name: checkout_main
      uses: actions/checkout@v2
      
    - name: build bogofilter
      run: |
        cd bogofilter-x
        ./default.configure; make
      env:
        OS: ${{ matrix.host }}
